# Dockerfile for user-service
FROM golang:1.17-alpine

WORKDIR /app
ADD . /app
#COPY . .
RUN ls *
#RUN go mod download
RUN go build ./payment-service/cmd/main.go 
RUN apt-get update && apt-get install -y \
curl
EXPOSE 8080
RUN ls *
CMD ["./main"]
